export type Server = {
  host: string;
  appServer: string;
  appServerId: number;
  appServerRelease: string;
  runtime: string;
  node: string;
  nodeId: number;
  nodeRelease: string;
  environment: string;
  environmentId: number;
  domain: string;
  domainId: string;
  definedOnNode: boolean;
};
